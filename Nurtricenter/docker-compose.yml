name: nurtricenter-ms3

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
    env_file:
      - .env
    ports:
      - "8080:8080"
